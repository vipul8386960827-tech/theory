/* 6_headings

1. Definition
HTML headings are elements used to define titles and subtitles on a webpage.  
They range from <h1> (highest importance) to <h6> (lowest importance) and help 
create document hierarchy and improve SEO and accessibility.

2. Key points to highlight
- There are 6 heading levels: <h1> to <h6>.
- <h1> is the main page heading (usually only one per page).
- They define structure, not just styling.
- Search engines give more weight to higher-level headings.
- Headings should follow a logical order: h1 → h2 → h3…
- Avoid skipping levels (e.g., h1 → h3) unless necessary.
- Useful for screen readers to navigate content.
- Should not be used only for styling; use CSS instead.

3. Example usage
<h1>Main Page Title</h1>
<h2>Section Title</h2>
<h3>Subsection Title</h3>
<h4>Minor Heading</h4>
<h5>Smallest Important Heading</h5>
<h6>Least Important Heading</h6>

4. Best practices
- Keep only one <h1> per page for SEO clarity.
- Use headings to build a clean content outline.
- Don’t use headings for bold/large text; use CSS (font-size, font-weight).
- Ensure meaningful text inside headings — avoid empty headings.

5. XHTML version (differences)
- Must be well-formed XML:
  <h1>Main Title</h1>
  <h2>Subtitle</h2>
- No empty headings allowed without closing tags.
- Tags must be lowercase (XML rule).

6. Follow-up interview questions
Q: How many <h1> tags should a page have?  
A: Ideally one, to maintain a clear document structure and better SEO.

Q: Does <h1> automatically make text large?  
A: Yes by default, but styling should be controlled via CSS.

Q: Are heading tags block-level or inline?  
A: Block-level elements.

Q: Can headings contain other elements?  
A: They can contain inline elements (e.g., <span>, <strong>) but not block-level elements.

Q: Do headings affect SEO?  
A: Yes, they help search engines understand page structure and content relevance.

*/
